# Environment Configuration Template

# Copy this to your repository and customize for your environments

# ====================
# SECRETS (Repository Settings → Secrets and variables → Actions → Secrets)
# ====================

# Required Secrets:
GOOGLE_CREDENTIALS='{"type": "service_account", "project_id": "your-project", ...}'
GCP_PROJECT_ID="your-gcp-project-id"
ALLOWED_SSH_CIDRS='["YOUR_IP/32"]'

# Optional Secrets:
INFRACOST_API_KEY="ico-xxxxxxxx"  # For cost estimation
SLACK_WEBHOOK_URL="https://hooks.slack.com/..."  # For notifications

# ====================
# VARIABLES (Repository Settings → Secrets and variables → Actions → Variables)
# ====================

# Infrastructure Configuration
GCP_REGION="us-central1"
GCP_ZONE="us-central1-a"
CLUSTER_NAME="secure-k8s"

# Node Configuration
MASTER_NODE_COUNT="1"
WORKER_NODE_COUNT="2"
MACHINE_TYPE="e2-medium"

# Network Configuration (optional - uses defaults if not set)
VPC_CIDR="10.0.0.0/16"
PRIVATE_SUBNET_CIDR="10.0.1.0/24"
PUBLIC_SUBNET_CIDR="10.0.2.0/24"

# ====================
# ENVIRONMENT-SPECIFIC VARIABLES
# ====================

# For Development Environment
DEV_MACHINE_TYPE="e2-small"
DEV_WORKER_COUNT="1"

# For Production Environment  
PROD_MACHINE_TYPE="e2-standard-2"
PROD_WORKER_COUNT="3"
PROD_MASTER_COUNT="3"  # HA setup for production